production:
  sessions:
    default:
      # Try all posible Heroku Mongo addons one after another
      uri: <%= ENV["MONGOHQ_URL"] || ENV["MONGOLAB_URI"] || ENV["MONGOSOUP_URL"] || ENV["MONGO_URL"] || "mongodb://localhost:27017/db" %>
