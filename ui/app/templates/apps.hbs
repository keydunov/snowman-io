{{#if showMenu}}
  <ul class="nav nav-tabs">
    {{#each app in model}}
      {{#link-to "apps.show" app tagName="li" href=false}}
        {{link-to app.name "apps.show" app}}
      {{/link-to}}
    {{/each}}
  </ul>
{{/if}}

{{outlet}}
