<form class="form-horizontal">
  <div class="form-group"}}>
    <label for="HGMetric" class="col-sm-2 control-label">HG Metric</label>
    <div class="col-sm-6">
      {{input type="text" value=hgMetric classNames="form-control" id="HGMetric" placeholder="Hosted Graphite metric, can be function like 'integral(some.value:sum)'"}}
    </div>
  </div>

  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-6">
      <button type="submit" class="btn btn-primary" {{bind-attr disabled=inProgress}} {{action "save"}}>Save</button>
    </div>
  </div>
</form>
