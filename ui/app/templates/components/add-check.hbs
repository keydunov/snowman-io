{{#if collapsed}}
  <a href="#" {{action "add"}}>Add Check</a>
{{else}}
  <span>Add Check</span> - <a href="#" {{action "cancel"}}>Cancel</a>
  <div class="well">
    <form class="form-inline">
      Snowman, notify when <b>{{hgMetric.name}}</b> will be 
      <div class="btn-group">
        <button class="btn btn-default btn-sm dropdown-toggle" type="button" data-toggle="dropdown">
          more <span class="caret"></span>
        </button>
        <ul class="dropdown-menu" role="menu">
          <li><a href="#">more</a></li>
          <li><a href="#">less</a></li>
        </ul>
      </div>
      <input type="text" class="form-control input-sm" value="0" />.
    </form>
  </div>
{{/if}}
